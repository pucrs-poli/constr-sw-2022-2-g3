name: Run All
on: [push]
jobs:
  run-all:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Create network
        run: npm run up:network
      - name: Run all
        run: npm run up
